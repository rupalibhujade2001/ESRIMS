server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rupalibhujade2001/ESRIMS.git
          default-label: main
          clone-on-start: true
