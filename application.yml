spring:
  jackson:
    default-property-inclusion: non_null

  mvc:
    problemdetails:
      enabled: true

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info