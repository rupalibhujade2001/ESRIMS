server:
  port: 8084
 
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/farm_management
    username: root
    password: Kadam@1999
  jpa:
    hibernate:
      ddl-auto: update
      
    show-sql: true
  data:
    redis:
       host: localhost
       port: 6379
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: inventory-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeSerializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.value.default.type: com.crop.inventory_service.kafka.event.ProductCreatedEvent

eureka:
   client:
     service-url:
       defaultZone: http://localhost:8761/eureka


       

jwt:
  secret: 4e7f8c9d2a1b6e5f7d8c9a0b1c2d3e4f5a6b7c8d9e0f123456789abcdef1234
  expiration: 3600000
      