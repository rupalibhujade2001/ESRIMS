function Inventory({ dashboard, isLiveDashboard, showNotice }) {
  return <div className="page-content inventory-page"><section className="inventory-heading"><div><p className="eyebrow">BULK SUPPLY MANAGEMENT</p><h1>Every lot, <i>ready to trade.</i></h1><p>{isLiveDashboard ? 'Live availability from GET /inventory/dashboard.' : 'Log in to load live availability; wholesale demo values are displayed now.'}</p></div><button className="primary-button" onClick={() => showNotice('Bulk stock update form is the next frontend feature.')}>Update available quantity</button></section><div className="inventory-summary"><div><span>Active lots</span><strong>{dashboard.totalProducts}</strong></div><div><span>Available to sell</span><strong>{dashboard.totalAvailableStock}</strong></div><div><span>Committed</span><strong>{dashboard.totalReservedStock}</strong></div><div><span>Sold out</span><strong className="red-text">{dashboard.outOfStockProducts}</strong></div></div><div className="panel inventory-table"><div className="panel-heading"><h3>Lots requiring an availability update</h3><button className="filter-button">Filter</button></div><div className="table-head"><span>Trade lot</span><span>Category</span><span>Available</span><span>Committed</span><span>Action</span></div>{dashboard.lowStockItems.map((item) => <div className="table-row" key={item.productId}><strong>{item.productName}</strong><span>{item.category}</span><span>{item.availableQuantity}</span><span>{item.reservedQuantity}</span><button onClick={() => showNotice(`Availability update opened for ${item.productName}.`)}>Update</button></div>)}</div></div>
}

export default Inventory
